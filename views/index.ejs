<!DOCTYPE html>
<html lang="en-US" dir="ltr">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <title>Socket | Pilot Bazar</title>
    <link href="/img/logo.svg" rel="shortcut icon" type="image/png" />
    <link href="/css/main.css" rel="stylesheet" />
</head>

<body class="antialiased">
    <div
        class="relative flex items-top justify-center min-h-screen bg-gray-100 dark:bg-gray-900 sm:items-center sm:pt-0">
        <div class="max-w-xl mx-auto sm:px-6 lg:px-8">
            <div class="flex items-center pt-8 sm:justify-start sm:pt-0">
                <div class="px-4 text-lg text-gray-500 border-r border-gray-400 tracking-wider">
                    <%= parse %>
                </div>
                <div class="ml-4 text-lg text-gray-500 tracking-wider">
                    <%= agent.os.name + ' ' + agent.os.version %>
                </div>
            </div>
            <div class="text-center text-gray-500">
                <%= agent.browser.name + ' ' + agent.browser.version %>
            </div>
        </div>
    </div>
</body>

</html>